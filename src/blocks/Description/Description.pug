include ../InfoCard/InfoCard
include ../ButtonLine/ButtonLine

mixin Description({cards, title} = {})
    section.description
        if title
            h2.description__title=title
        ul.description__cards
            each card in cards
                li.description__cards-item
                    +InfoCard(card)

