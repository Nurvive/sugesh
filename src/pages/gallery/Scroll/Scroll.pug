mixin Scroll({images = [], title = "", text = ""} = {})
    .scroll.js-scroll
        ul.scroll__list
            each img in images
                li.scroll__list-item
                    img(src=img)
